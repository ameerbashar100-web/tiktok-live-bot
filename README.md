# Tiktok Live Bot - UI mockup

This repository now contains a static HTML mockup of the mobile app UI you provided screenshots for. The page is an Arabic RTL simulation of the dashboard, menus, and plans cards.

Files added:
- `index.html` — static HTML/CSS copy of the UI screenshots.

Notes:
- You asked to use the exact images and icons you sent. I included emoji placeholders and inline SVG/icons in `index.html` for compatibility. If you want the original screenshots included in the repository, please upload the image files (JPEG/PNG). I can then add them to an `assets/` folder and update the HTML to reference them.
- To update or add assets: upload images here in the repository or send them in the chat and I will commit them for you.

How to preview locally:
1. Clone the repo: `git clone https://github.com/ameerbashar100-web/tiktok-live-bot.git`
2. Open `index.html` in your browser.

If you want, I can now add the exact screenshots into `assets/` and update the HTML to use them. Reply with the images (or confirm to allow me to use the images you posted in the chat) and I will add them in a follow-up commit.
