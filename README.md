# BonChat — Messages UI

Static GitHub-ready recreation of the Messages screen shown in the reference image.

- Opens directly on the Messages screen.
- No server URL screen.
- No login.
- No database.
- Mobile responsive.
- Static front-end only.

Upload `index.html`, `styles.css`, `app.js`, and `assets/` to GitHub Pages or another static host.
